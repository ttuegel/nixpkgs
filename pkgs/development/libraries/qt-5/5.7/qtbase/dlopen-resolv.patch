Index: qtbase-opensource-src-5.7.1/src/network/kernel/qdnslookup_unix.cpp
===================================================================
--- qtbase-opensource-src-5.7.1.orig/src/network/kernel/qdnslookup_unix.cpp
+++ qtbase-opensource-src-5.7.1/src/network/kernel/qdnslookup_unix.cpp
@@ -90,7 +90,7 @@ static bool resolveLibraryInternal()
     if (!lib.load())
 #endif
     {
-        lib.setFileName(QLatin1String("resolv"));
+        lib.setFileName(QLatin1String("@glibc@/lib/resolv"));
         if (!lib.load())
             return false;
     }
Index: qtbase-opensource-src-5.7.1/src/network/kernel/qhostinfo_unix.cpp
===================================================================
--- qtbase-opensource-src-5.7.1.orig/src/network/kernel/qhostinfo_unix.cpp
+++ qtbase-opensource-src-5.7.1/src/network/kernel/qhostinfo_unix.cpp
@@ -100,7 +100,7 @@ static bool resolveLibraryInternal()
     if (!lib.load())
 #endif
     {
-        lib.setFileName(QLatin1String("resolv"));
+        lib.setFileName(QLatin1String("@glibc@/lib/libresolv"));
         if (!lib.load())
             return false;
     }
